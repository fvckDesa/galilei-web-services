{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "scripts": {
    "dev": "cargo watch -x \"run --bin api\"",
    "build": "cargo build --release",
    "start": "cargo run --release",
    "lint": "cargo clippy -- -D warnings",
    "lint:fix": "cargo clippy -- -D warnings"
  }
}
