{
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": [
        "API_BASE_URL",
        "HOST_PUBLIC_DOMAIN",
        "HOST_PUBLIC_PORT",
        "HOST_PRIVATE_DOMAIN",
        "WEB_HOST_DOMAIN"
      ]
    }
  }
}
