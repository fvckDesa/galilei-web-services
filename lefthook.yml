pre-commit:
  piped: true
  commands:
    lint:
      glob: "*.{ts,tsx,rs}"
      run: pnpm lint:fix
    fmt:
      glob: "*"
      run: pnpm fmt

pre-push:
  commands:
    test:
      glob: "*.{ts,tsx,rs}"
      run: pnpm test
